services:
  mcp-planka:
    build:
      context: .
    env_file:
      - .env
    restart: unless-stopped
    # stdio mode (default): no ports needed
    # For HTTP mode, add:
    # ports:
    #   - "8000:8000"
    # And override CMD in docker run